name: sentiment-analysis-playstore

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "playstore_reviews_preprocessing/clean_reviews.csv"}
    command: "python modelling.py --data_path {data_path}"
